*.tfstate filter=lfs diff=lfs merge=lfs -text
# Enforce LF for all files
* text=auto eol=lf

# Explicitly enforce LF for shell scripts
*.sh text eol=lf

# Enforce LF for Terraform files
*.tf text eol=lf
*.tfvars text eol=lf
*.hcl text eol=lf

# Ignore local terraform state files
*.tfstate binary
*.tfstate.backup binary
